# === ACCESS CONVERTER KONFIGURATION ===
# Diese Datei zeigt alle verfügbaren Umgebungsvariablen für den Access Converter
# Kopieren Sie diese Datei nach .env und passen Sie die Werte nach Bedarf an

# === GRUNDKONFIGURATION ===
LOG_LEVEL=INFO
# Optionen: DEBUG, INFO, WARNING, ERROR

# === DATEIGRÖSSEN-LIMITS ===
# Maximum Upload-Größe in Bytes
MAX_UPLOAD_SIZE=104857600
# Vordefinierte Größen:
# 50MB  = 52428800
# 100MB = 104857600  (Standard)
# 200MB = 209715200
# 500MB = 524288000
# 1GB   = 1073741824
# 2GB   = 2147483648

# === PERFORMANCE-EINSTELLUNGEN ===
# Anzahl der gleichzeitigen Konvertierungen
MAX_CONCURRENT_JOBS=3
# Empfehlung: 1-2 pro CPU-Kern

# Bereinigungsintervall in Minuten
CLEANUP_INTERVAL_MINUTES=60
# Empfehlung: 30-120 Minuten

# === ERWEITERTE EINSTELLUNGEN ===
# Maximum Anzahl Tabellen pro Datenbank
MAX_TABLES_PER_DB=100

# Temporäres Verzeichnis Größenlimit in Bytes
TEMP_DIR_SIZE_LIMIT=1073741824
# Standard: 1GB

# Worker Timeout in Sekunden
WORKER_TIMEOUT=300
# Standard: 5 Minuten

# === NETZWERK-KONFIGURATION ===
# Externer Port für den Web-Service
EXTERNAL_PORT=8080

# === VOLUME-KONFIGURATION ===
# Uncomment für Host-Bind-Mounts statt Docker Volumes
# UPLOADS_PATH=/volume1/docker/access-converter/uploads
# EXPORTS_PATH=/volume1/docker/access-converter/exports
# LOGS_PATH=/volume1/docker/access-converter/logs

# === BEISPIEL-KONFIGURATIONEN ===

# Kleine Umgebung (begrenzte Ressourcen):
# MAX_UPLOAD_SIZE=52428800          # 50MB
# MAX_CONCURRENT_JOBS=1
# CLEANUP_INTERVAL_MINUTES=30
# TEMP_DIR_SIZE_LIMIT=536870912     # 512MB

# Große Umgebung (Server/NAS):
# MAX_UPLOAD_SIZE=2147483648        # 2GB
# MAX_CONCURRENT_JOBS=5
# CLEANUP_INTERVAL_MINUTES=120
# TEMP_DIR_SIZE_LIMIT=5368709120    # 5GB

# Entwicklungsumgebung:
# LOG_LEVEL=DEBUG
# MAX_UPLOAD_SIZE=209715200         # 200MB
# MAX_CONCURRENT_JOBS=2
# CLEANUP_INTERVAL_MINUTES=15
# WORKER_TIMEOUT=600                # 10 Minuten
